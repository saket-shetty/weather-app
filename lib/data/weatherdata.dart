import 'package:flutter/material.dart';

class weatherdata{
  var date;
  var month;
  var temperature;
  var weatherdescription;
  IconData icondata;

  weatherdata(this.date, this.month, this.temperature, this.weatherdescription, this.icondata);
}