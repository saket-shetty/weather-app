import 'package:flutter/material.dart';

class weatherdata{
  var date;
  var time;
  var month;
  var temperature;
  var weatherdescription;
  IconData icondata;

  weatherdata(this.date, this.time, this.month, this.temperature, this.weatherdescription, this.icondata);
}